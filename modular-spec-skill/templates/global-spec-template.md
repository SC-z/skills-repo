# Global Architecture Spec

> 本文档由 modular-spec-skill 自动生成  
> 用于描述系统整体架构、模块职责划分及其协作关系  
> 目标读者：项目开发者 / 维护者 / Reviewer / 新接手成员

---

## 1. 项目背景与目标（Context & Goals）

### 1.1 项目背景
- 项目解决的核心问题：
- 当前所处阶段（初期 / 迭代期 / 稳定期 / 重构期）：
- 主要使用场景与用户类型：

### 1.2 设计目标
- 系统级核心目标（性能 / 稳定性 / 可扩展性 / 可维护性等）：
- 明确 **非目标（Non-Goals）**：
  - 本系统不试图解决的问题
  - 有意延后或规避的复杂性

---

## 2. 系统整体架构概览（High-Level Architecture）

### 2.1 架构风格
- 架构类型（单体 / 分层 / 模块化 / 微服务 / 事件驱动等）：
- 关键设计思想：
  - 解耦策略
  - 依赖方向约束
  - 状态与控制的分布方式

### 2.2 架构分层（如适用）
```text
[ 接口层 / API / CLI ]
        ↓
[ 业务协调层 / Orchestration ]
        ↓
[ 核心模块层 ]
        ↓
[ 基础设施 / 依赖组件 ]
```

> 若非严格分层结构，请描述逻辑分区与边界约束。

---

## 3. 模块全景视图（Module Landscape）

### 3.1 模块列表

| 模块名 | 类型 | 核心职责 | 依赖方向 |
|------|------|----------|----------|
| xxx  | 核心模块 | ... | → yyy |
| yyy  | 基础模块 | ... | → zzz |

> 类型示例：核心 / 支撑 / 基础设施 / 适配层 / 工具类

### 3.2 模块关系总览
- 模块之间的主要调用链路
- 是否存在：
  - 双向依赖
  - 隐式耦合
  - 共享状态或全局资源

---

## 4. 核心数据与控制流（Global Flow）

### 4.1 主流程说明
- 系统启动或请求处理的整体路径：
  1. 从哪里进入系统
  2. 经过哪些关键模块
  3. 在哪里完成核心决策
  4. 在哪里落地结果

### 4.2 关键数据对象
- 系统级核心数据结构：
  - 生命周期
  - 所有权归属
  - 是否跨模块共享

---

## 5. 模块边界与职责原则（Boundary & Responsibility）

### 5.1 模块划分原则
- 划分依据：
  - 业务能力
  - 技术职责
  - 演进频率
- 明确避免的反模式：
  - 上帝模块
  - 逻辑漂移
  - 跨层直连

### 5.2 边界约束
- 哪些模块 **不允许** 直接交互
- 哪些依赖是“被容忍但不推荐”的

---

## 6. 关键设计决策（Key Design Decisions）

> 记录 **“为什么这样设计”**，而不仅是“怎么实现”。

- 决策 1：
  - 背景：
  - 可选方案：
  - 最终选择：
  - 取舍与代价：

- 决策 2：
  - …

---

## 7. 演进方向与系统风险（Evolution & Risks）

### 7.1 当前已知风险
- 架构层面的潜在问题
- 复杂度增长点
- 性能或稳定性隐患

### 7.2 演进建议
- 未来可能的拆分 / 合并方向
- 哪些模块是优先重构候选
- 哪些约束在未来可能需要放松或强化

---

## 8. 与模块 Spec 的关系说明

- 本文档为 **全局视角**
- 每个模块的详细设计、接口、内部流程：
  - 详见：`spec/modules/<module-name>.md`
- 全局 Spec 的任何修改：
  - 必须与模块 Spec 保持一致

---

## 9. 阅读与维护约定（Usage & Maintenance）

- 新成员阅读顺序建议：
  1. 本 Global Spec
  2. 核心模块 Spec
  3. 目标模块源码
- 本文档更新触发条件：
  - 新增 / 删除模块
  - 架构级调整
  - 关键设计决策变化
